### Trained model checkpoints:

* `best_2016_gcn.pth`: Graph convolution network (GCN) trained on the 2016-2017 time sub-series.
* `best_2016_gat.pth`: Graph attention network (GAT) trained on the 2016-2017 time sub-series.
* `best_2021_gcn.pth`: Graph convolution network (GCN) trained on the 2021-2022 time sub-series.
* `best_2021_gat.pth`: Graph attention network (GAT) trained on the 2021-2022 time sub-series.

To use an existing checkpoint, copy it to `../best_load_forecasting_gcn.pth` for use with cell no. 6 of the notebook.
